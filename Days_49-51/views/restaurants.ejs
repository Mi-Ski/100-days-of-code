<!DOCTYPE html>
<html lang="en">
  <head><%- include("includes/head") %> 
		<link rel="stylesheet" href="styles/restaurants.css" />
	</head>
</head>
	<body>
    <%- include('includes/header') %> 
    <%- include('includes/aside') %> 
		<main>
			<h1>Recommended restaurants</h1>
			<% if(restaurants.length === 0) { %>
			<p>Unfortunately we don't have any restaurants yet. Please add some!</p>
			<% } else { %> 
			<p>Find your next favorite restaurants with help of our other users!</p>
			<p>We found <%= numberOfRestaurants %> restaurants.</p>
			<% } %> 
			<ul id="restaurants-list">
				<% for (const el of restaurants) { %>
					<%- include('includes/restaurants/restaurant-item.ejs', {restaurant: el}) %> 
				<% }; %>
			</ul>
		</main>
	</body>
</html>
